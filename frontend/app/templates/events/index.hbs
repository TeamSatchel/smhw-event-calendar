<h2>Events</h2>

<AddEventButton @newEvent={{this.newEvent}} @save={{action "save"}} />

<ul>
  {{#each model as |event|}}
    <li>{{event.description}} {{event.startDate}} {{event.endDate}}</li>
  {{/each}}
</ul>
