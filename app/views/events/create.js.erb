<% if @event.new_record? %>
    alert("<%= @event.errors.messages.keys.map{ |key| "#{key} - #{@event.errors.messages[key].join('; ')}" }.join('; ').html_safe %>");
<% else %>
    $('#event_title').val('');
<% end %>
