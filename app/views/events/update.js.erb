// remove original event and then render new event
$('#calendar').fullCalendar('removeEvents', [<%= @event.id %>]);
$('#calendar').fullCalendar(
    'renderEvent',
    $.parseJSON("<%=j render(@event, format: :json).html_safe %>"),
    true
);
$('.modal').modal('hide');